const { AbilityBuilder, Ability } = require("@casl/ability");

const policies = {
    
    guest(user, { can }){
        
    },

    customer(user, { can }){

    },

    waiter(user, { can }){

    },

    chef(user, { can }){
        
    },

    cashier(user, { can }){

    }

}